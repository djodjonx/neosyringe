name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18, 20, 22]

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Build packages (Node 20+)
        if: matrix.node-version != 18
        run: pnpm build

      - name: Lint
        run: pnpm lint

      - name: Type check
        run: pnpm typecheck

      - name: Test (Node 18)
        if: matrix.node-version == 18
        run: pnpm test

      - name: Test with coverage (Node 20+)
        if: matrix.node-version != 18
        run: pnpm test:coverage

      - name: Upload coverage to Codecov
        if: matrix.node-version == 20
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/clover.xml
          fail_ci_if_error: false

  build:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Build
        run: pnpm build

      - name: Verify build output
        run: |
          # Core
          test -f packages/core/dist/analyzer/index.mjs
          test -f packages/core/dist/generator/index.mjs
          # Library
          test -f packages/neosyringe/dist/index.mjs
          test -f packages/neosyringe/dist/index.cjs
          test -f packages/neosyringe/dist/index.d.mts
          # Plugin
          test -f packages/unplugin/dist/index.mjs
          test -f packages/unplugin/dist/index.cjs
          # LSP
          test -f packages/lsp/dist/index.cjs
          # CLI
          test -f packages/cli/dist/index.mjs

  security:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Audit dependencies
        run: pnpm audit --audit-level=high
        continue-on-error: true

